# neovim_configs
My neovim configs customised for golang.

First you should have vim-plug installed on your linux system
After install latest LTS nodejs
Then add files to ~/.config/nvim/
Then $nvim init.vim and run :PlugInstall command in normal mode
